# TODO

- [ ] Manual test `yolo cleanup` across projects (running, stopped, worktree, empty dir cases)
- [ ] Allow configuring forwarded ports in `.yolo/` (so `--port` flags don't need to be repeated every time)
